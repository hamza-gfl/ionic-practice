import {FC} from "react";
import {IonFooter, IonText} from "@ionic/react";

const Footer: FC = () => {
    return <IonFooter className="ion-text-center ion-padding">
        <IonText> All rights reserved. </IonText>
    </IonFooter>
}

export default Footer